{
  "name": "@lwrf42/emerge-example-eval-probes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@lwrf42/emerge-kernel": "workspace:*",
    "@lwrf42/emerge-provider-mock": "workspace:*",
    "@lwrf42/emerge-surveillance": "workspace:*"
  }
}
